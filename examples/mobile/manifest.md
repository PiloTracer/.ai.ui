# mobile — manifest

**Archetype:** `mobile-app` · **Patterns doc:** UI-PATTERNS § mobile + navigation

| id | file | patterns (verified) | use when |
|----|------|---------------------|----------|
| M1 | `image.png` | 3-up: home feed + editor canvas + AI bottom sheet; bottom nav + center FAB; horizontal category chips; template carousels | Consumer creative / content apps |
| M2 | `image copy.png` | Onboarding flow; full-bleed illustration per step; progress dots | Swipe or next button to advance; skip link top-right; get-started CTA on final step | Headline bold centered; body centered below; step indicator dots | Illustration upper 60%; text lower 40%; dots centered above CTA | Onboarding: 3–5 screens max; illustration + headline + body per step; skip always visible; progress dots indicate position; final step CTA prominent; skip goes to same destination as completing all steps | `Button`, `TextLink`, `Illustration` (slot) |
| M3 | `image copy 2.png` | Profile / settings screen; grouped list sections; avatar header | Avatar tap to change photo; list rows with chevron disclosure; toggle rows for preferences; sign-out at bottom destructive | Section headers caps muted; row labels regular; values right-aligned muted | Avatar + name header card; sections separated by spacing; list rows full-width | Profile/settings: grouped sections with headers; disclosure rows for drill-down; toggle rows for boolean prefs; destructive actions (sign out, delete) at bottom with red text; avatar change via action sheet (camera/library); version number at very bottom muted | `Card`, `Toggle`, `Avatar`, `SectionHeader`, `Button` |
| M4 | `image copy 3.png` | Search screen; recent searches; trending/suggested chips; results list | Search bar auto-focused on entry; clear button in field; recent search rows with clock icon; result rows with thumbnail | Search bar placeholder muted; recent items regular; result titles bold | Search bar top; recent section; trending chips row; results list below | Search: auto-focus input on screen entry; recent searches with delete-individual; trending as horizontal chip scroll; results show thumbnail + title + subtitle; empty state suggests search terms; cancel dismisses keyboard + returns to previous | `SearchBar`, `Chip`, `Card`, `IconButton` |
| M5 | `image copy 4.png` | Detail screen; hero image; scrollable content; sticky bottom CTA | Share and bookmark icon buttons in nav; image gallery horizontal scroll; expandable description; sticky bottom bar with price + CTA | Title bold large; price bold accent; description regular; metadata muted | Hero image full-bleed; content padded below; bottom bar fixed with safe area | Detail page: hero image with gallery dots; title + price + rating below; expandable description (3-line clamp + "Read more"); metadata tags; sticky bottom CTA bar — price left, button right; share + bookmark in top nav | `Button`, `IconButton`, `Card`, `Chip`, `Gallery` (pattern) |
| M6 | `image copy 5.png` | List / feed screen; pull-to-refresh; card stream with actions | Pull-to-refresh spinner; card swipe actions (archive, delete); like/comment icon buttons; FAB for new post | Card title bold; body two-line clamp; timestamp relative muted | Cards full-width; FAB bottom-right above safe area; action icons row below card content | Feed: pull-to-refresh with spinner; cards with image + title + excerpt + timestamp + action row; swipe-to-archive with undo toast; FAB for primary create; infinite scroll with loading skeleton at bottom; empty feed: follow suggestions | `Card`, `Button`, `IconButton`, `Skeleton` |
| M7 | `image copy 6.png` | Notification center; grouped by date; read/unread states | Swipe to dismiss; tap navigates to source; mark-all-read button in header; filter tabs (all/unread) | Notification title bold when unread; body regular; timestamp right-aligned muted | Full-width list rows; date section headers; filter tabs at top | Notifications: grouped by date; bold title for unread, regular for read; swipe to dismiss with undo; tap navigates to relevant screen; mark-all-read in header; filter tabs (all / unread / mentions); empty state when no notifications | `SectionHeader`, `Tabs`, `Button`, `TextLink` |
| M8 | `image copy 7.png` | Checkout / payment flow; step indicator; form fields; order summary | Step indicator (shipping → payment → review); address form with autocomplete; payment method selector; place-order CTA | Step labels medium weight; form labels visible above fields; total bold large | Step indicator top; form fields padded; order summary card bottom or collapsible | Checkout flow: linear step indicator (no skipping); address form with autocomplete; payment methods as radio cards (visa icon + last 4); order summary collapsible on mobile; place-order CTA disabled until valid; error handling per field inline | `Stepper` (pattern), `Card`, `RadioGroup`, `Button`, `TextInput` |
| M9 | `image copy 8.png` | Empty / error state collection; illustration + message + CTA patterns | Retry button on error; CTA on empty; back/home navigation | Headline centered bold; body centered muted; CTA below | Illustration centered upper; text group centered; CTA centered below text | Empty/error states: illustration + headline + body + CTA; error: "Something went wrong" + retry; empty list: explain what would appear + CTA to create; no connection: show cached data if available + banner; permission denied: explain what access is needed + request CTA | `Button`, `Illustration` (slot), `TextLink` |

**Extracted rules:**

- **Mode switch:** browse (tabs + lists) vs edit (canvas + tool tray) — different chrome
- Bottom sheet for AI/secondary tasks; keep canvas visible
- Horizontal scroll for categories/templates with clear affordance
- FAB for primary create — do not stack with 5th tab item without SPEC

**Pair with:** `mobile-controls/manifest.md` for control sizing.
