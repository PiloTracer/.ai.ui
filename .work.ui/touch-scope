{
  "description": "Touch scope — 2026-08-19 operator-directed: remove ui-deploy-repo skill (skills/ui-deploy-repo/, scripts/ui-deploy-repo.sh) for consistency with the deploy family (Agent OS and pilo.ai.logicbison ship deploy-basic + deploy-files only); sweep every reference (skills/README.md, SKILL_DEPENDENCIES.md, ui-director skill+reference, ui-deploy-files skill.md + shell exclude regex, framework-verify.sh check-list + smoke tests, cursorrules-verify.sh header, templates/cursorrules.ui.template, .quick/deploy-to-project.md); record session in HANDOFF_UI/NEXT_UI/CHANGELOG [Unreleased]; run all four gates. Follow-up (same message thread): doc-only refresh of remaining /mnt/work/Projects absolute examples in shipped docs — scripts/ui-deploy-basic.sh (usage comment L34 + AI_UI_SOURCE help row L202) and templates/cursorrules.ui.template L157 → portable /path/to/.ai.ui. reasonix.toml (host config) and docs/homogenization/ui.md (dated implementation record) intentionally left.",
  "blast_radius_approved": true,
  "blast_radius_note": "Operator-authorized in same message ('Remove deploy-repo skill, for consistency it must be removed'). Removal inherently spans skills/scripts/templates/.quick/.work.ui/root CHANGELOG — every reference must be swept or framework-verify fails (prose-count + registry cross-checks). Minimal diffs: registry rows and verifier references only; historical CHANGELOG/HANDOFF entries left intact.",
  "allowed_paths": [
    ".cursorrules",
    "agent.os.framework.md",
    ".gitignore",
    "README.md",
    "START_HERE.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "APPROACH.md",
    "COHABITATION.md",
    "PROCESS_ROUTER.md",
    "context/README.md",
    ".quick/",
    "skills/",
    "resources/",
    "standards/",
    "concepts/",
    "templates/",
    "docs/",
    ".github/",
    "hooks/",
    "scripts/",
    ".work.ui/"
  ],
  "allowed_patterns": [
    "*.md",
    "*.json",
    ".cursorrules",
    ".gitignore",
    "hooks/*",
    "*.sh",
    "*.yml",
    "*.yaml",
    "*.css",
    "*.py",
    "*.template"
  ],
  "rules": [
    "Only touch files under allowed_paths",
    "Only touch files matching allowed_patterns",
    "If you need to touch a file outside scope, update this file first"
  ]
}
