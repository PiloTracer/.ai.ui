# Framework audit — UI Design OS (`.ai.ui`)

Structural audit of the UI Design OS framework repo per `.work.ui/prompts/framework-audit.md`, verifying today's contract adoption and sweeping for drift.

**Status:** Complete · 2026-08-14 (fixes applied, operator-approved same-day)
**Needs:** nothing — all findings resolved or dispositioned; see § Resolution.

## Scope

- Repo: `/mnt/work/Projects/.ai.ui` (git root; framework at repo root, demo memory in `.work.ui/`).
- Spec: `.work.ui/prompts/framework-audit.md` (8 checks, read-only).
- Verified adoption claims from 2026-08-14: both contracts in `skills/SKILL_DEPENDENCIES.md`, 19/19 skill.md handoff refs, 7 doc-generating clarity refs, `.cursorrules` + both cursorrules templates, 14 `templates/work.ui/` doc templates, `scripts/framework-verify.sh` `scan_contracts`.

## Per-check results

### 1. Registry congruence — PASS

- 19 `skills/ui-*/` directories on disk; all 19 appear in the `skills/README.md` registry table (`skills/README.md:27`-`47`) and all 19 are named in `skills/SKILL_DEPENDENCIES.md` (matrix/verb tables, e.g. `skills/SKILL_DEPENDENCIES.md:49`-`77`, `109`-`136`).
- Root `.cursorrules` has **no skills table** (by design — it registers `skills/README.md` as the registry in Required reads, `.cursorrules:55`). This matches the audit note; not a gap. `framework-verify.sh` independently derives "19 skills registered in skills/README.md" (run output line 68).
- Every skill dir has a `skill.md` (verified 19/19).

### 2. Operator handoff adoption — PASS

- Section present: `skills/SKILL_DEPENDENCIES.md:151` (`## Operator handoff contract`).
- 19/19 `skills/*/skill.md` reference the contract (grep for "Operator handoff"; enforced by `scripts/framework-verify.sh:87`, which passed).
- `.cursorrules:39` (`## Communication (operator handoff)`), `templates/cursorrules.ui.template:301` (`### Operator handoff (communication)`), `templates/cursorrules.ui.snippet.template:29` (`### UI operator handoff (communication)`) — all present.
- Spot-check of the 5 skills that got report-template close lines — each closes its report template with "Close per the Operator handoff contract (Form A if nothing is needed; Form B with **Needs your approval:** / **Needs your answer:** / **Next step:** otherwise)": `skills/ui-copy/skill.md:263`, `skills/ui-visual-verify/skill.md:66`, `skills/ui-accessibility-audit/skill.md:42`, `skills/ui-plan-verify/skill.md:53`, `skills/ui-project-approach/skill.md:64`. Exactly these 5 skills carry the close line (grep count = 5).
- Note (`inference`): the close line instructs Form A/Form B rather than instantiating a literal Form A/B string in each template; this satisfies the contract as enforced (`skills/SKILL_DEPENDENCIES.md:162`) but means the forms are only rendered at runtime.

### 3. Document clarity adoption — PASS (with one classified observation)

- Section present: `skills/SKILL_DEPENDENCIES.md:166` (`## Document clarity contract`).
- All 7 claimed doc-generating skills reference it: `ui-design-foundation`, `ui-screen-spec`, `ui-component-build`, `ui-copy`, `ui-design-system`, `ui-visual-verify`, `ui-accessibility-audit` (grep "Document clarity"; enforced by `scripts/framework-verify.sh:93`, passed).
- Exactly 14 `templates/work.ui/` files carry `**Status:**` + `**Needs:**` + exactly one `## Next action` — claim verified: `context/HANDOFF_UI.md.template`, `decisions/README.md.template`, `design-system/CATALOG.md.template`, `plans/ASSUMPTIONS.md.template`, `plans/foundation/03-design-system.brand.template.md`, `plans/foundation/PROBE_LEDGER.md.template`, `plans/foundation/YYYYMMDD-01..04-*.template` (4), `plans/NEXT_UI.md.template`, `plans/RISK_REGISTRY.md.template`, `plans/UNKNOWNS.md.template`, `screens/example-slug/YYYYMMDD-SCREEN-SPEC.md.template`.
- Skipped (0 headers, as intended): `templates/work.ui/README.md.template`, `plans/foundation/README.md.template`, `plans/full/README.md.template`, `prompts/README.md.template`, `screens/README.md.template`, `touch-scope.template`.
- Classified finding F3: `decisions/README.md.template` **is** an index README yet received the header/Next-action treatment while the other 5 index READMEs were skipped — the "index READMEs skipped" rule was applied inconsistently (`confirmed`, low severity; count of 14 is still accurate).

### 4. Routing docs — PASS

- `framework-verify.sh` full markdown local-link scan passed: "ok: markdown local-link scan (no broken relative links)" (run output line 118).
- Manual spot-check: every relative link target in `README.md` (`APPROACH.md`, `COHABITATION.md`, `START_HERE.md`, `LICENSE`, `docs/adoption/FROM_AGENT_OS.md`, `examples/INDEX.md`, `resources/*`, `skills/README.md`, `standards/20260523-*.md`, `style-stacks/README.md`, `.work.ui/README.md`) and `PROCESS_ROUTER.md` (`skills/SKILL_DEPENDENCIES.md`, `skills/ui-process-router/skill.md` + `reference.md`, `START_HERE.md`) exists. `START_HERE.md` spot-checks (`examples/INDEX.md`, `standards/20260523-SURFACE-AND-CONTROL-CRAFT.md`, `style-stacks/README.md`, `skills/ui-visual-verify/skill.md`) also exist.

### 5. Templates ↔ .cursorrules sync — PASS

- Root `.cursorrules` Required-reads paths all exist (`.cursorrules:50`-`56`; confirmed by verifier output lines 4–28).
- Change-safety gates in root `.cursorrules:68`-`73` use unprefixed `scripts/…` paths and `templates/work.ui/touch-scope.template` (`.cursorrules:75`) — consistent with the documented path convention for this repo as git root (`.cursorrules:60`); the adopter template correctly uses `.ai.ui/`-prefixed paths (`templates/cursorrules.ui.template:59`-`62`) and the adopter `.work.ui/` layout.
- Template skills table is an explicitly labeled subset pointing to `skills/README.md` (`templates/cursorrules.ui.template:171`-`187`); no drift vs the registry.
- Communication/operator-handoff sections present in all three files (see check 2).

### 6. Hooks — PASS with one finding

- All 4 mandated hooks present: `hooks/commit-msg`, `hooks/post-commit`, `hooks/pre-commit`, `hooks/prepare-commit-msg`.
- `scripts/install-git-hooks.sh:29` references exactly these 4; no stale references to removed scripts found. `.cursorrules:109` points to the installer.
- Classified finding F1 (`confirmed`, low severity): hook sources are **not executable** — filesystem mode `664`, git index mode `100644` for all four. Impact is mitigated: the installer `chmod +x`s the copies at install time (`scripts/install-git-hooks.sh:37`) and the verifier self-test "install-git-hooks copies hooks (4 installed)" passes. Still, the spec asks for "present and executable", so flagged.

### 7. Protected surfaces — FAIL (one real gap)

- Existing lists: `.cursorrules:88`-`101` ("Data loss & destructive operations", incl. overwrite of `.ai/`, adopter `.work/`, `.cursorrules` wholesale at `.cursorrules:99`); `templates/cursorrules.ui.template:212`-`226` ("Protected Files", REPLACE-token paths — appropriately adopter-specific); `templates/cursorrules.ui.snippet.template:81`-`85` ("UI protected files"). Root `.cursorrules` having no file-level Protected Files list is consistent with the template design (token/config paths are adopter REPLACE tokens).
- Classified finding F2 (`confirmed`): `agent.os.framework.md:6` declares itself protected citing **`standards/PROTECTED_SURFACES.json`** and **`.cursorrules §Protected Files`** — neither exists. `standards/PROTECTED_SURFACES.json` is absent from disk (no JSON under `standards/` at all), and the root `.cursorrules` has no "Protected Files" section (only the adopter template does). Additional signal: `agent.os.framework.md` is itself **untracked** in git (`git status`: `?? agent.os.framework.md`), and a same-day prompt also assumes the JSON exists (`.work.ui/prompts/20260814-marker-detection-deploy-repo-removal-audit.md:13`). Intent of the file is `unknown` — it may be an orphaned work-in-progress.
- Coverage note (`inference`): no existing list covers framework high-blast paths such as `hooks/` or `scripts/framework-verify.sh`; only `.cursorrules`-wholesale, `.ai/`, and adopter `.work/` overwrites are guarded (`.cursorrules:99`).

### 8. Verifiers — PASS for framework-verify; touch-scope FAILS (see F4)

- `bash scripts/framework-verify.sh` (from repo root): **exit 0**, `framework-verify: PASS`. 100+ `ok:` lines including: clarity contracts ("2 sections + 19 handoff refs + doc-generating clarity refs"), all scan_contracts self-tests (accept/reject), markdown link scan, bootstrap-test, deploy self-tests, install-git-hooks self-test.
- `scripts/touch-scope-verify.sh`, `scripts/blast-radius-check.sh`, `scripts/gate-verify.sh` all exist; gate-verify self-tests pass inside framework-verify (output lines 111–112).
- `bash scripts/touch-scope-verify.sh`: **exit 1 — FAIL**: "files outside declared .work.ui/touch-scope: `agent.os.framework.md`, `.cursorrules`".
  - Classified finding F4a (`confirmed`): `agent.os.framework.md` is untracked and not in `allowed_paths` (`.work.ui/touch-scope` lists specific root files but not this one) — same root cause as F2.
  - Classified finding F4b (`confirmed`): `.cursorrules` **is** in `allowed_paths` yet still fails, because the verifier requires BOTH a path and a pattern match (`scripts/touch-scope-verify.sh:91` uses `||`) and `.cursorrules` matches no entry in `allowed_patterns` (`*.md`, `*.json`, `.gitignore`, `*.sh`, `*.yml`, `*.yaml`, `*.css`, `*.py`, `*.template` — no extensionless-dotfile entry). So today's `.cursorrules` edit is in-scope by intent but the scope file + verifier logic combination rejects it.

## Findings summary

| Id | Class | Finding | Severity |
|----|-------|---------|----------|
| F1 | confirmed | `hooks/*` mode 664 / git 100644 — not executable at source (installer chmods at install: `scripts/install-git-hooks.sh:37`) | Low |
| F2 | confirmed | `agent.os.framework.md:6` cites nonexistent `standards/PROTECTED_SURFACES.json` and a `.cursorrules §Protected Files` section the root file lacks; the file itself is untracked | Medium |
| F3 | confirmed | `templates/work.ui/decisions/README.md.template` treated despite "index READMEs skipped" rule — inconsistent application (count of 14 still correct) | Low |
| F4a | confirmed | touch-scope FAIL: untracked `agent.os.framework.md` outside `allowed_paths` (overlaps F2) | Medium |
| F4b | confirmed | touch-scope FAIL: `.cursorrules` in `allowed_paths` but matches no `allowed_patterns` entry; verifier ANDs path+pattern (`scripts/touch-scope-verify.sh:91`) | Medium |
| F5 | inference | No protected-files list covers `hooks/` or verifier scripts; only tree-overwrite rules exist (`.cursorrules:99`) | Low |
| F6 | unknown | Ownership/intent of `agent.os.framework.md` (orphan WIP vs intended protected surface) | — |

## Proposed fixes (dispositioned — see § Resolution)

1. F2/F4a/F6: decide on `agent.os.framework.md` — either (a) create `standards/PROTECTED_SURFACES.json` listing the framework high-blast paths (e.g. `agent.os.framework.md`, `.cursorrules`, `hooks/`, `scripts/framework-verify.sh`, `standards/`), add a short "Protected Files" section to the root `.cursorrules` pointing at it, and `git add` the file; or (b) delete the file as orphan WIP (requires operator decision — intent unknown).
2. F4b: add `.cursorrules` to `allowed_patterns` in `.work.ui/touch-scope` (one-line change) — or, more durably, treat exact `allowed_paths` file matches as sufficient in `scripts/touch-scope-verify.sh` (larger behavioral change; prefer the one-line scope fix).
3. F1: `chmod +x hooks/*` and `git update-index --chmod=+x hooks/*` so source hooks are executable as the audit spec expects.
4. F3: either strip the Status/Needs header + `## Next action` from `templates/work.ui/decisions/README.md.template` for consistency with the other 5 index READMEs, or document decisions/README as a deliberate exception in the template README.
5. F5 (optional, with fix 1a): include `hooks/` and verifier scripts in the new `PROTECTED_SURFACES.json` coverage.

## Resolution (2026-08-14, operator-approved)

| Id | Disposition |
|----|-------------|
| F1 | **Resolved** — `chmod +x` + `git update-index --chmod=+x` on all 4 hooks; git index now `100755`. |
| F2/F4a/F6 | **Resolved (operator direction)** — operator reinstated `agent.os.framework.md` as the intended framework source marker (cross-framework convention shared with Agent OS `.ai`). Original content was inaccurate here (claimed "Agent OS … self-hosted `.ai`"; cited nonexistent lists) and nothing consumed it, so it was made truthful and wired: content corrected to UI Design OS (`.ai.ui`); `standards/PROTECTED_SURFACES.json` created (also resolves F5); `.cursorrules` §Protected Files added; `ui-session.sh` framework-mode detection now uses the marker (3-file heuristic kept as fallback); `ui-deploy-files.sh` excludes it from deploys; wiring enforced by `framework-verify.sh`. |
| F3 | **Resolved by design** — `decisions/README.md.template` is the ADR registry document, not a pure index; header treatment was intentional. |
| F4b | **Resolved** — `.cursorrules` added to `allowed_patterns` in `.work.ui/touch-scope`. |
| F5 | **Resolved** — `standards/PROTECTED_SURFACES.json` (created for F2) covers `hooks/` and the verifier scripts; `.cursorrules` §Protected Files points at it. |

Post-fix gates: `framework-verify.sh` PASS, `touch-scope-verify.sh` PASS, `blast-radius-check.sh` PASS, `gate-verify.sh` PASS (all run 2026-08-14 after resolutions).

## Next action

Next action: none — all findings resolved or dispositioned; all four change-safety gates green.
