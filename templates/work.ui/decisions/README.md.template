# UI decisions (optional)

**Status:** Draft · **Needs:** REPLACE:<decision, review, or nothing>

Prefer shared ADRs in `.work/decisions/` with `ui-` slug prefix when Agent OS is present.

Format: `YYYYMMDD-NNN-ui-<slug>.md`

## Next action

Next action: none — record a UI ADR here only when a decision needs it (`YYYYMMDD-NNN-ui-<slug>.md`)
